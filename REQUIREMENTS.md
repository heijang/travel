# Travel Map — 개발 요구사항

> 최종 수정: 2026-08-28
> 상태: 초안 (Phase 1 착수 전)

## 1. 개요

여행 일정을 지도 위에 시각화하는 웹 서비스. 6박 7일 같은 다일차 여정을 Day 단위로 나눠
이동 경로를 지도에 그리고, 각 지역의 숙박·명소·활동을 마커로 표시한다. 마커를 클릭하면
상세 정보와 외부 링크(예약 페이지, 공식 사이트, 지도 앱)로 이동한다.

**Phase 1의 데이터는 저장된 정적 JSON을 사용한다.** 사용자 입력 기능은 Phase 2로 미룬다.
단, Phase 1의 데이터 스키마는 Phase 2에서 DB 테이블로 그대로 옮길 수 있도록 설계한다.

**대상 여정: 여행자의 실제 일정** — 2026-08-17(월) ~ 08-23(일), 6박 7일.
오슬로 공항 → 트롤퉁가 → 베르겐 → 플롬/스테가스테인 → 튀인크뤼세트 → 오슬로 공항.
데이터는 `src/data/norway-fjords.json`에 있고, 자세한 내용은 7절에 있다.

## 2. 기술 스택 (확정)

| 영역 | 선택 | 비고 |
|---|---|---|
| 지도 엔진 | MapLibre GL JS | 오픈소스, API 키 불필요, 3D terrain 지원 |
| 베이스 타일 | 무료 벡터 타일 (OpenFreeMap 또는 Versatiles) | 키 불필요. 유료 전환 시 style URL만 교체 |
| 지형(선택) | Terrarium/AWS Terrain Tiles | 피오르 지형 입체 표현용, Phase 1.5 |
| 프레임워크 | React 19 + TypeScript + Vite | |
| 상태 관리 | React 상태 + URL 쿼리스트링 | 별도 라이브러리 없이 시작 |
| 스타일 | CSS Modules 또는 Tailwind | 구현 시 결정 |
| 데이터 | `src/data/*.json` (정적) | 백엔드 없음 |
| 배포 | 정적 호스팅 (Vercel / Netlify / GitHub Pages) | |

**결정 근거**: API 키·과금 없이 즉시 시작할 수 있고, 벡터 타일이라 확대/축소가 매끄러우며,
노르웨이 피오르 지형을 3D로 표현할 여지가 있다. 나중에 Mapbox로 옮겨야 해도 MapLibre는
Mapbox GL JS v1의 포크라 API가 거의 호환된다.

## 3. 데이터 모델

```ts
// 여행 전체
interface Trip {
  id: string;                 // "norway-fjords-2026"
  title: string;              // "노르웨이 피오르 6박 7일"
  startDate: string;          // ISO 날짜 "2026-06-15"
  nights: number;             // 6
  region: string;             // "Southwest Norway"
  initialView: {              // 지도 최초 진입 시 뷰포트
    center: [number, number]; // [lng, lat] — MapLibre는 lng 먼저
    zoom: number;
    bearing?: number;
    pitch?: number;
  };
  days: Day[];
}

// 하루 일정
interface Day {
  dayNumber: number;          // 1..7
  date: string;               // ISO 날짜
  title: string;              // "오슬로 도착 & 시내"
  summary?: string;           // 1~2문장 요약 (객관적 일정 설명)
  color: string;              // 경로/마커 색 (Day별 고유 색)
  route?: Route;              // 그날의 이동 경로. 이동 없는 날은 생략
  places: Place[];            // 그날 방문/숙박 지점들
  notes?: Note[];             // 하루 전체에 대한 메모
}

// 이동 경로
interface Route {
  mode: TransportMode;        // 주 이동수단
  distanceKm?: number;
  durationMin?: number;
  // Phase 1: 수동으로 넣은 좌표 배열 (도로 형상 근사)
  // Phase 2: 라우팅 API 결과 polyline으로 대체
  geometry: [number, number][]; // [lng, lat][]
  legs?: RouteLeg[];          // 페리 구간 등 이동수단이 섞일 때
}

interface RouteLeg {
  mode: TransportMode;
  from: string;               // Place.id
  to: string;                 // Place.id
  geometry: [number, number][];
  note?: string;              // "Lauvvik-Oanes 페리 20분"
}

type TransportMode = 'car' | 'ferry' | 'train' | 'bus' | 'walk' | 'hike' | 'flight';

// 지점 (숙소 / 명소 / 식당 / 활동)
interface Place {
  id: string;
  name: string;
  nameLocal?: string;         // 현지 표기 "Trolltunga"
  type: PlaceType;
  coordinates: [number, number]; // [lng, lat]
  dayNumber: number;
  order: number;              // 그날 안에서의 방문 순서
  description?: string;
  imageUrl?: string;
  // 숙박 전용
  checkIn?: string;           // ISO 날짜
  checkOut?: string;
  nights?: number;
  // 부가 정보
  time?: string;              // 시각 "HH:MM". 시각이 중요한 지점에만
  durationMin?: number;       // 체류/소요 시간
  priceNote?: string;         // "NOK 1,800 / 1박"
  tags?: string[];            // ["하이킹", "왕복 8시간", "난이도 상"]
  links: Link[];
  notes?: Note[];             // 이 장소에 대한 메모
}

type PlaceType = 'stay' | 'sight' | 'hike' | 'food' | 'transport' | 'activity';

interface Link {
  label: string;              // "예약 페이지", "공식 사이트", "구글 지도"
  url: string;
  type: 'booking' | 'official' | 'map' | 'info' | 'review';
}

// 작성자 본인이 남기는 메모. Place와 Day 두 단위에 붙는다.
interface Note {
  kind: NoteKind;
  text: string;               // 마크다운 아님, 플레인 텍스트. 줄바꿈만 허용
}

type NoteKind =
  | 'warning'   // 주의 — "비 오면 바위가 미끄러움", "9월 이후 폐쇄"
  | 'tip'       // 꿀팁 — "새벽 4시 출발해야 일출을 봄", "주차는 P2가 비어 있음"
  | 'note';     // 일반 — "여기가 제일 기대되는 곳"
```

### 설계 노트
- 좌표는 `[lng, lat]` 순서로 통일 (MapLibre/GeoJSON 규약). 실수하기 쉬우니 타입 별칭
  `type LngLat = [number, number]`를 두고 주석을 남긴다.
- `Place.dayNumber`를 Place에도 중복해서 넣는 이유: "전체 보기" 모드에서 Day를 순회하지 않고
  평탄한 배열로 다룰 때 편하다.
- Phase 2에서 이 구조는 `trips` / `days` / `places` / `links` / `notes` 5개 테이블로 그대로
  매핑된다.
- `notes`를 단일 문자열이 아니라 **배열**로 둔 이유: 한 장소에 성격이 다른 메모가 여러 개
  붙는 경우가 실제로 흔하다(트롤퉁가 = 주의 하나 + 꿀팁 둘). 배열이면 Phase 2에서 여러 명이
  다는 댓글로 확장할 때 `author`, `createdAt` 필드만 추가하면 되고 구조는 안 바뀐다.
- `Day.summary`와 `Day.notes`는 역할이 다르다. `summary`는 "그날 무엇을 하는지"에 대한
  객관적 설명이고, `notes`는 "그래서 내가 뭘 알아야 하는지"에 대한 주관적 메모다. 섞지 않는다.

## 4. 기능 요구사항

### 4.1 지도 표시 (FR-MAP)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-MAP-1 | 진입 시 여행 전체 범위가 한눈에 들어오도록 bounds를 자동 fit 한다 | 必 |
| FR-MAP-2 | 팬/줌/회전이 가능하고, "전체 보기로 리셋" 버튼을 제공한다 | 必 |
| FR-MAP-3 | Day별 경로를 서로 다른 색의 라인으로 그린다 | 必 |
| FR-MAP-4 | 이동수단에 따라 라인 스타일을 다르게 한다 (차=실선, 페리=파선, 기차=점선, 하이킹=굵은 점선) | 必 |
| FR-MAP-5 | 각 Place를 마커로 표시하고, `type`별로 아이콘을 다르게 한다 | 必 |
| FR-MAP-6 | 숙박(`stay`) 마커는 다른 마커보다 크게, 숙박 박수 배지를 함께 표시한다 | 必 |
| FR-MAP-7 | 마커가 겹치는 줌 레벨에서 클러스터링하거나 오프셋을 준다 | 中 |
| FR-MAP-8 | 경로 위에 진행 방향 화살표를 표시한다 | 中 |
| FR-MAP-8a | 화살표는 Day 색으로 채우고 흰 테두리를 둘러, 실선 위든 파선의 빈 구간이든 배경과 무관하게 읽히게 한다 | 中 |
| FR-MAP-9 | 메모가 있는 마커에 배지를 붙여 메모의 존재를 알린다 (상세는 4.5 참조) | 必 |
| FR-MAP-11 | 사진이 있는 숙소는 마커를 원형 사진으로 표시한다. 사진 로드에 실패하면 아이콘으로 되돌린다 | 中 |
| FR-MAP-10 | 3D terrain(피오르 지형)을 토글로 켜고 끌 수 있다 | 低 |

### 4.2 Day 네비게이션 (FR-DAY)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-DAY-1 | 화면 상단 또는 좌측에 Day 1..7 탭/칩을 배치한다 | 必 |
| FR-DAY-2 | Day를 선택하면 해당 Day의 경로·마커만 강조하고 나머지는 흐리게(opacity 0.25) 처리한다 | 必 |
| FR-DAY-3 | Day 선택 시 그 Day의 bounds로 카메라가 부드럽게 이동한다 (`flyTo` / `fitBounds`) | 必 |
| FR-DAY-4 | "전체" 탭에서 모든 Day를 동시에 표시한다 (기본값) | 必 |
| FR-DAY-5 | 이전/다음 Day 이동 버튼과 좌우 방향키 단축키를 제공한다 | 中 |
| FR-DAY-6 | 선택된 Day가 URL에 반영된다 (`?day=3`) — 링크 공유·새로고침 유지 | 中 |
| FR-DAY-7 | Day 1→7을 자동 재생하는 "여정 애니메이션" 모드 | 低 |

### 4.3 장소 상세 (FR-PLACE)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-PLACE-1 | 마커 클릭 시 팝업/사이드패널에 상세 정보를 띄운다 | 必 |
| FR-PLACE-2 | 상세에 이름·유형·설명·체류시간·가격 메모·태그를 표시한다 | 必 |
| FR-PLACE-3 | `links` 배열을 버튼 목록으로 렌더링하고, 새 탭(`target="_blank" rel="noopener noreferrer"`)으로 연다 | 必 |
| FR-PLACE-4 | 마커 hover 시 이름 툴팁을 보여준다 | 中 |
| FR-PLACE-5 | 이미지가 있으면 상세 상단에 썸네일을 표시한다 | 中 |
| FR-PLACE-6 | 패널의 리스트 항목 ↔ 지도 마커가 양방향으로 연동된다 (리스트 hover → 마커 강조, 마커 클릭 → 리스트 스크롤) | 中 |

### 4.4 일정 패널 (FR-PANEL)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-PANEL-1 | 지도 옆에 선택된 Day의 일정을 순서대로 나열하는 패널을 둔다 | 必 |
| FR-PANEL-2 | 각 항목에 순서 번호·시간·이동수단 아이콘을 표시한다 | 必 |
| FR-PANEL-3 | Day 헤더에 날짜·제목·요약·총 이동거리/시간을 표시한다 | 中 |
| FR-PANEL-4 | 패널을 접었다 펼 수 있다 (지도 전체화면) | 中 |
| FR-PANEL-5 | 연박 숙소는 `checkIn <= 그날 < checkOut` 조건으로 골라 **매일** 패널에 표시한다. 데이터에는 체크인 날 하루치만 들어 있다 | 必 |

### 4.5 메모 (FR-NOTE)

작성자 본인이 각 장소와 각 Day에 남기는 메모. **Phase 1은 읽기 전용**으로, 메모는 JSON에
미리 작성해 둔다. 화면에서 입력·수정하는 기능은 Phase 2에서 장소 추가 기능과 함께 넣는다.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-NOTE-1 | `Place.notes`를 상세 팝업과 일정 패널 양쪽에 **전문 그대로** 표시한다 (말줄임 없음) | 必 |
| FR-NOTE-2 | `Day.notes`를 일정 패널의 Day 헤더 아래에 표시한다 | 必 |
| FR-NOTE-3 | `kind`에 따라 색·아이콘을 다르게 한다 — warning: 주황/⚠, tip: 파랑/💡, note: 회색/📝 | 必 |
| FR-NOTE-4 | 메모가 있는 마커에 작은 배지를 붙인다. 마커 하나에 메모가 여러 개면 가장 강한 `kind` 하나만 배지로 노출한다 (**warning > tip > note**) | 必 |
| FR-NOTE-5 | 메모의 줄바꿈(`\n`)을 렌더링에 반영한다 (`white-space: pre-wrap`) | 必 |
| FR-NOTE-6 | 메모는 플레인 텍스트로만 렌더링한다. HTML/마크다운을 해석하지 않는다 | 必 |
| FR-NOTE-7 | 메모가 없는 장소/Day에서는 관련 UI 영역을 아예 렌더링하지 않는다 (빈 블록·"메모 없음" 문구 금지) | 必 |
| FR-NOTE-8 | 패널에서 메모가 3줄을 넘으면 접고 "더 보기"를 제공한다 | 中 |
| FR-NOTE-9 | `warning` 메모가 있는 Day는 Day 탭에도 작은 표시를 남긴다 (해당 Day를 열기 전에 알 수 있도록) | 中 |
| FR-NOTE-10 | 메모만 모아 보는 필터/목록 뷰 ("이 여행의 주의사항 전부 보기") | 低 |

**FR-NOTE-6 관련**: Phase 1에선 메모를 내가 직접 쓰니 위험이 없지만, Phase 2에서 입력 기능이
붙는 순간 이 규칙이 XSS 방어선이 된다. 지금부터 `dangerouslySetInnerHTML`을 쓰지 않는 것으로
못 박아 둔다.

### 4.6 투어 화면 (FR-TOUR)

기본 화면. 자동차가 경로를 따라 움직이며 주요 지점에 도착할 때마다 그곳이 어디인지
보여준다. 일자별 상세 동선은 필요할 때만 `일정표` 버튼으로 연다.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-TOUR-1 | 7일 전체를 끊김 없는 하나의 경로로 잇고 그 위를 아이콘이 이동한다 | 必 |
| FR-TOUR-2 | 정차 지점은 **숙박·명소·하이킹·식사·액티비티**만. `transport`(공항·터널·주차장·역)는 경유지라 제외한다 | 必 |
| FR-TOUR-3 | 도착하면 멈춰서 카드로 장소·설명·메모·링크를 보여준다 | 必 |
| FR-TOUR-4 | 카메라가 이동 아이콘을 따라간다. 이동수단에 맞는 줌을 쓴다 | 必 |
| FR-TOUR-5 | 경로 전체를 처음부터 Day 색으로 **연하게** 그려 두고, 지나간 구간만 **진하게** 바꾼다. 회색으로 두지 않는다 — 시작 전에도 여정 전체의 모양과 날짜 구성이 읽혀야 한다 | 必 |
| FR-TOUR-6 | 재생/일시정지, 이전·다음 지점, 처음부터, 배속(1×/2×/4×), 전체 보기 | 必 |
| FR-TOUR-7 | 하단 진행 막대의 점을 누르면 그 지점으로 바로 이동한다 | 必 |
| FR-TOUR-8 | 이동수단에 따라 아이콘이 바뀐다 (🚗 🥾 🚆 🚶) | 中 |
| FR-TOUR-9 | 사용자가 지도를 끌면 재생을 멈춘다 (카메라와 다투지 않게) | 中 |
| FR-TOUR-10 | 스페이스바 재생/정지, 좌우 방향키로 지점 이동 | 中 |

### 4.7 데이터 (FR-DATA)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-DATA-1 | 여행 데이터는 `src/data/norway-fjords.json` 한 파일에서 읽는다 | 必 |
| FR-DATA-2 | 앱 시작 시 스키마를 검증하고, 실패하면 콘솔에 어떤 필드가 문제인지 출력한다 (Zod 등) | 中 |
| FR-DATA-3 | 여행 데이터를 여러 개 둘 수 있는 구조로 로더를 작성한다 (`?trip=norway-fjords`) | 中 |
| FR-DATA-4 | 언더스코어(`_`)로 시작하는 키는 로더가 무시한다 (`_meta` = 데이터 출처·주의사항 기록용) | 中 |

## 5. 비기능 요구사항

- **성능**: 초기 로드 3초 이내(3G Fast 기준), 지도 인터랙션 60fps 유지. 데이터 규모는
  1개 여행 = 최대 100 places 수준을 가정.
- **반응형**: 데스크톱은 지도+사이드패널 2단, 모바일(<768px)은 지도 전체화면 + 하단
  드래그 시트. 모바일이 후순위가 아니라 동등한 1급 대상.
- **접근성**: 마커는 키보드 탭 이동 가능, 팝업은 ESC로 닫힘, 색만으로 Day를 구분하지 않고
  번호 라벨을 병기.
- ~~**다크모드**~~: **제거됨 (2026-08-30).** 가독성이 떨어진다는 판단으로 라이트 한 벌만 쓴다.
  팔레트·토글·`setStyle` 전환 로직을 모두 걷어냈다.
- **브라우저**: 최신 Chrome/Safari/Firefox/Edge. WebGL 미지원 시 안내 문구 표시.
- **i18n**: Phase 1은 한국어 UI. 장소명은 한국어 + 현지어 병기. 문자열은 하드코딩하지 않고
  한 곳에 모아 나중에 분리 가능하게.

## 6. 화면 구성

```
데스크톱 (>=1024px)
┌──────────────────────────────────────────────────────────┐
│  노르웨이 피오르 6박 7일        [전체][D1][D2]...[D7]  🌓 │
├───────────────────────────────┬──────────────────────────┤
│                               │  Day 3 · 6월 17일        │
│                               │  오다 → 트롤퉁가          │
│           지도                │  🚗 82km · 1시간 40분    │
│      (MapLibre GL)            │  ⚠ 이날은 하루를 통째로  │
│                               │  ──────────────────────  │
│   ● 마커 / ━ Day별 경로       │  ① 🥾 트롤퉁가 트레일    │
│   ●⚠ 메모 배지 달린 마커      │      왕복 10-12시간      │
│                               │      ⚠ 9월 중순 이후 폐쇄│
│                               │      💡 새벽 4시 출발    │
│                               │      [공식] [주차예약]   │
│                               │  ② 🏨 트롤퉁가 호텔      │
│  [전체보기] [3D]              │      1박 · NOK 1,800     │
│                               │      [부킹닷컴]          │
└───────────────────────────────┴──────────────────────────┘

모바일 (<768px)
지도 전체화면 + 상단 Day 칩 가로 스크롤 + 하단 드래그 시트(일정 리스트)
```

### 메모 표시 규칙
- 마커 배지는 마커 우상단에 붙는 작은 원형 아이콘. 지도가 지저분해지지 않도록 배지 하나만,
  아이콘만 표시한다 (텍스트 없음).
- 팝업과 패널에서는 메모를 `kind`별 좌측 컬러바가 있는 블록으로 표시한다.
- 메모 블록은 링크 버튼보다 **위**에 둔다. 링크를 누르기 전에 주의사항을 먼저 읽어야 한다.

## 7. 여정 데이터 — 실제 일정 (2026-08-29 반영)

여행자가 제공한 **실제 일정**이다. 이전의 가상 코스와 내가 임의로 넣었던 경유지
(오슬로 시내 명소, 예일로, 에이드피오르, 스테인달스포센, 보스, 레르달 터널, 보르군
스타브 교회, 베르겐 시내 관광 지점)는 요청에 따라 **전부 삭제했다.** 지금 데이터에는
여행자가 말한 지점만 들어 있다.

**2026년 8월 17일(월) ~ 23일(일), 6박 7일 · 총 1,336km**

| Day | 날짜 | 구간 | 이동 | 숙박 |
|---|---|---|---|---|
| 1 | 8/17 (월) | 오슬로 공항 11:30 도착 → 트롤퉁가 입구 (익일 00:00 도착) | 🚗 389.8km · 6시간 52분 (Rv7 북쪽) | Trolltunga 입구 (2박) |
| 2 | 8/18 (화) | 트롤퉁가 왕복 하이킹 | 🥾 28km · 11시간 | 〃 |
| 3 | 8/19 (수) | → 뵈링포센 → 베르겐 | 🚗 262.5km · 4시간 44분 | Bergen (2박) |
| 4 | 8/20 (목) | 베르겐 (**지점 미정**) | — | 〃 |
| 5 | 8/21 (금) | → 플롬 → 스테가스테인 → 튀인크뤼세트 | 🚗 275.5km · 4시간 40분 | Tyinkrysset (2박) |
| 6 | 8/22 (토) | → 플롬, 플롬스바나 탑승 | 🚗 94.1km + 🚆 | 〃 |
| 7 | 8/23 (일) | → 오슬로 공항 18:30 출발 | 🚗 256.1km · 4시간 17분 (E16 북쪽) | — |

투어 정차 지점은 7곳이다 (`transport` 유형인 공항 2곳과 Day 5의 플롬 경유는 제외).

### 좌표 출처

숙소 3곳을 뺀 모든 좌표는 **Nominatim(OpenStreetMap)에서 조회해 확인**했다.

| 지점 | 좌표 `[lng, lat]` |
|---|---|
| Oslo lufthavn Gardermoen | `[11.0997, 60.1979]` |
| Skjeggedal (트롤퉁가 입구) | `[6.6248, 60.1324]` |
| Trolltunga | `[6.7540, 60.1330]` |
| Vøringsfossen | `[7.2509, 60.4271]` |
| Bergen | `[5.3259, 60.3943]` |
| Flåm | `[7.1132, 60.8630]` |
| Stegastein | `[7.2118, 60.9086]` |
| Myrdal 역 | `[7.1228, 60.7352]` |
| Tyinkrysset | `[8.2372, 61.2035]` |

### 오슬로 ↔ 서쪽 이동은 반드시 북쪽 경로

여행자 확인: 오슬로에서 서쪽으로 넘어간 두 번 모두 **북쪽 길**로 갔고 남쪽(E134
하우켈리) 길은 한 번도 쓰지 않았다.

OSRM은 기본적으로 최단 시간인 남쪽 E134를 고른다. 그래서 Day 1 경로에
**예일로 `[8.2064, 60.5333]`를 경유지로 넣어** Rv7(하르당에르비다 고원)로 강제했다.
Day 7(튀인크뤼세트 → 오슬로)은 E16 파게르네스 경유라 원래부터 북쪽이다.

경로 위도로 검증한 결과:

| 구간 | 위도 범위 | 최남단 |
|---|---|---|
| Day 1 오슬로 → 트롤퉁가 | 60.12 ~ 60.57 | `[6.57, 60.12]` (오다 도착점) |
| Day 7 튀인크뤼세트 → 오슬로 | 60.18 ~ 61.23 | `[11.08, 60.18]` (가르데르모엔 자체) |

남쪽 E134 경로였다면 위도 59.5 부근까지 내려간다. 둘 다 60.1 아래로 내려가지 않으므로
북쪽 경로가 맞다. **경유지를 지우면 Day 1이 다시 남쪽으로 돌아간다.**

### 재생 속도

초기 속도(자동차 14km/s)가 너무 느리다는 피드백으로 약 3배 올렸다.
자동차 42, 버스 36, 페리 18, 기차 6, 하이킹 4.5, 도보 1 (km/초). 따라가기 줌도
자동차 8.3 → 7.6으로 낮춰 화면에 더 넓은 범위가 들어오게 했다.
프레임 루프 시뮬레이션 기준 1배속 58초, 4배속 32초 (이전에는 1배속 약 3분).

### 이동 중에는 이전 카드를 띄우지 않는다

이동 중에도 직전 정차지 카드가 흐리게 남아 있어, 화면이 이미 지나온 장소를 설명하는
상태가 됐다. 정차 중(`arrived`)일 때만 카드를 띄우고, 이동 중에는 대신 **다음 정차지**를
알리는 작은 알약(`Next · <이름>`)을 보여준다. 400km를 달리는 동안 화면이 비지 않으면서도
낡은 정보를 보여주지 않는다.

### 아직 확정되지 않은 것

1. **숙소 좌표** — 튀인크뤼세트는 여행자가 주소를 알려줘 확정했다
   (Eidsbugardvegen 2, 2985 Tyinkrysset → `[8.23334, 61.23157]`). 나머지 둘(트롤퉁가
   입구, 베르겐)은 여전히 근사치이며 각 숙소 메모에 경고로 표시돼 있다.
2. **Day 4(베르겐)에 지점이 없다.** 여행자가 정하면 채운다. 그날은 투어에서 정차하지 않는다.
3. **설명문은 임시다.** 여행자가 다시 쓰기로 했다.
4. **연도는 2026으로 넣었다.** 오늘이 2026-08-29이므로 8/17~23은 지난 주다. 다른 해라면
   `startDate`와 각 Day의 `date`를 바꿔야 한다.

### 사진 (10개 지점 전부)

`npm run images`(scripts/fetch-photos.mjs)가 각 지점의 사진을
`public/photos/<place.id>.jpg`로 내려받고 `Place.imageUrl`을 채운다.
`--dry`는 변경 내용만, `--force`는 이미 받은 파일도 다시 받는다.

출처가 둘이다.

| 대상 | 출처 | 저작자 표시 |
|---|---|---|
| 숙소 3곳 | 예약 링크의 Open Graph 미리보기 | 없음 (본인 예약 건) |
| 나머지 7곳 | `Place.wikipedia` 문서의 대표 이미지 (Wikimedia Commons) | **필수** |

**Commons 이미지는 라이선스가 붙어 있다** (CC BY 2.0/3.0, CC BY-SA 3.0/4.0). 스크립트가
저작자와 라이선스를 함께 받아 `Place.imageCredit`에 넣고 사진 아래에 표시한다. 둘 중
하나라도 못 받으면 **그 사진은 쓰지 않는다.** 출처 없이 싣지 않기 위한 규칙이다.

현재 표시되는 저작자: kallerna(트롤퉁가), Netha Hussain(뵈링포센), TomasEE(스테가스테인),
Kenny Louie(플롬), Karen(플롬스바나), Avinor/Espen Solli(오슬로 공항).

**CDN을 직접 링크하지 않고 내려받는 이유**: 예약 사이트 URL에는 사진별 id가 들어 있어
호스트가 숙소를 편집하면 바뀌고, 어느 쪽도 교차 출처 제공이 계속되리라는 보장이 없다.

표시 위치는 세 곳 — 지도 마커(숙소는 원형), 투어 카드(상단 전체 폭), 장소 상세 팝업.
가로세로비가 제각각(4:3, 3:2, 세로 3:4, 와이드)이라 컨테이너마다 비율을 고정하고
`object-fit: cover`로 잘라낸다. 로드에 실패하면 마커는 아이콘으로 돌아가고 카드·팝업은
사진 영역(저작자 표시 포함)을 통째로 제거한다.

**Wikimedia API는 429를 자주 돌려준다.** 지점당 두 번 호출하는데 연달아 치면 걸린다.
제한은 짧고 응답이 `Retry-After`로 대기 시간을 알려주므로, 그 값을 읽어 기다렸다
재시도한다(최대 5회). 브라우저를 사칭하지 않고 프로젝트를 밝히는 User-Agent를 쓴다 —
Wikimedia 정책이 요구한다.

#### 사진을 붙이며 밟은 두 가지

1. **분리된 `<img>`에 `loading="lazy"`를 걸면 영원히 로드되지 않는다.** 지연 로딩은
   뷰포트 진입을 기다리는데, 문서에 붙지 않은 요소는 뷰포트에 들어갈 일이 없다.
   처음에 "로드되면 붙인다"로 짰다가 교착에 빠져 마커가 계속 아이콘으로 남았다.
   지금은 **먼저 붙이고 실패하면 되돌린다.**
2. **`.marker--stay .marker__pin`(0,2,0)이 `.marker__pin--photo`(0,1,0)를 이긴다.**
   크기가 적용되지 않았고, 게다가 `place-items: center` 때문에 이미지가 콘텐츠 크기로
   남아 `height:100%`가 풀리지 않아 세로 사진이 박스를 29×39로 늘렸다. 선택자
   우선순위를 맞추고 사진 핀은 `display: block`으로 바꿔 해결했다.

### 투어의 마지막 지점 = 공항

`transport` 유형은 경유지라 투어에서 건너뛰지만, 여행의 끝은 공항이어야 한다.
`Place.tourStop`(`true`/`false`)으로 유형별 기본 동작을 덮어쓸 수 있게 하고
출국 공항에 `true`를 줬다. 투어 정차 지점은 7곳 → **8곳**이 됐고 마지막이 공항이다.

Day 6은 플롬스바나를 타고 **같은 숙소로 돌아와** 하룻밤 더 자고, Day 7에 공항으로 떠난다.
경로상으로는 되짚기 로직이 이미 그렇게 처리하고 있었지만 화면에 드러나지 않아,
Day 6의 요약·메모·leg 설명에 명시했다.

### 시간 표기

`Place.time`(`"HH:MM"`)을 추가했다. 시각이 실제로 중요한 지점에만 넣는다 — 지금은
공항 도착 11:30, 트롤퉁가 입구 도착 00:00, 공항 출발 18:30 셋이다. 일정 패널과 투어
카드의 메타 줄 맨 앞에, 장소 상세에는 별도 행으로 표시된다. (기존 `durationMin`은
상세에서 라벨이 Time → Duration으로 바뀌었다.)

### 경로 지오메트리

`waypoints`만 손으로 관리하고 `npm run routes`가 `geometry`를 만든다. 자동차 구간은
OSRM에서 실제 도로 형상과 거리·소요시간을 받아오고, 하이킹(트롤퉁가)과 철도(플롬스바나)는
라우팅이 불가능해 손으로 찍은 근사 좌표를 그대로 쓴다.

되짚기 처리 결과: 끊긴 곳 2군데(트롤퉁가 하이킹 복귀, 플롬스바나+플롬 복귀)가 모두
역방향 구간으로 메워졌고 직선 연결은 필요하지 않았다. 최종 경로 2,001점.

## 8. 개발 단계

### Phase 1 — 정적 지도 뷰어 ✅ 완료 (배포 제외)
1. ✅ Vite + React 19 + TS 스캐폴딩, MapLibre 연동
2. ✅ 타입 정의 + JSON 로더 (7 Day, 현재 10 places — 실제 일정)
3. ✅ 마커 렌더링 (type별 아이콘, Day별 색, 숙박 박수 배지)
4. ✅ Day별 경로 라인 (mode별 실선/파선/점선)
5. ✅ Day 탭 + 카메라 이동 + 강조/디밍 + 방향키 + URL 동기화
6. ✅ 장소 상세 팝업 + 링크 버튼
7. ✅ 사이드 일정 패널 + 리스트↔마커 양방향 연동 + 연박 이월
8. ✅ 메모 렌더링 (kind별 블록 + 마커 배지 + 접기)
9. ✅ 반응형(모바일 바텀시트) + WebGL 폴백 (다크모드는 이후 제거)
9a. ✅ 진행 방향 화살표 (FR-MAP-8) — Phase 1.5 항목이었으나 선반영
9b. ✅ 투어 화면 (FR-TOUR) — 기본 화면으로 채택. 일자별 상세는 `일정표` 토글로 유지
10. ⬜ **정적 배포 — 미실행.** 외부 공개라 별도 승인 후 진행.

#### 구현 중 발견해 해결한 문제
- **마커가 지도 좌표를 무시하고 세로로 쌓임** — `styles.css`가 `maplibre-gl.css`보다
  뒤에 로드되면서 `.marker { position: relative }`가 `.maplibregl-marker`의
  `position: absolute`를 덮었다. 같은 좌표의 마커 두 개가 다른 위치에 찍혀 드러났다.
  마커 바깥 요소의 `position`/`transform`은 MapLibre 전용으로 두고, 확대·투명도는
  `.marker__inner`로 분리했다.
- **초기 화면이 한참 축소된 상태로 뜸** — 컨테이너 크기가 0일 때 지도를 만들면
  MapLibre가 400×300을 가정하고 그 크기로 `fitBounds`를 계산한다. 컨테이너에 크기가
  잡힐 때까지 기다렸다 생성한다.
- **백그라운드 탭에서 지도가 영영 생성되지 않음** — 생성 지연에 `requestAnimationFrame`을
  쓰면 숨겨진 탭에서 콜백이 실행되지 않는다. 타이머로 교체했다.
- **개발 모드에서 타일이 멈춤** — StrictMode의 이중 마운트로 지도를 만들자마자
  `remove()`하면 MapLibre 워커 풀 정리와 두 번째 생성이 겹쳐 타일 처리가 멈춘다.
  생성을 한 틱 미뤄 첫 마운트에서는 아예 만들지 않는다.
- **활성 Day 탭 색이 hover 색에 덮임** — `.daytab:hover`(0,2,0)가
  `.daytab--active`(0,1,0)보다 우선순위가 높다. 활성 탭을 hover 대상에서 제외했다.
- **모바일에서 경로가 하단 시트 뒤로 숨음** — `fitBounds` 여백이 시트 높이를 반영하도록
  했다.
- **`load` 이벤트가 오지 않음** — liberty 스타일의 음영기복 래스터가 무거워 `load`가
  늦거나 안 온다. 레이어를 붙일 수 있는 시점인 `style.load`를 준비 신호로 쓴다.

#### 투어 경로를 만드는 방법

7일치 route를 leg 단위로 펴서 순서대로 이으면 **경로가 끊긴다**. 트롤퉁가 하이킹,
플롬스바나, 스테가스테인은 갔다가 돌아오는 구간인데 데이터에는 편도만 있기 때문이다.
그대로 두면 자동차가 순간이동한다.

해결: 다음 구간 시작점이 1km 넘게 떨어져 있으면 **방금 지나온 구간들을 역순으로 되짚어
붙인다**(최대 4개까지). 실제로 걸어 내려오는 길이라 자연스럽고, 되짚는 구간은 3.5배속으로
빠르게 지나간다. 데이터로 검증한 결과 끊김 4곳이 모두 되짚기로 메워졌고 직선 연결은
한 번도 필요하지 않았다. 최종 경로는 1,726점 · 1,128km.

정차 지점을 경로 위에 매핑할 때 두 가지 함정이 있었다.

- **순환 코스라 같은 도시를 두 번 지난다.** 경로 전체에서 최근접점을 고르면 오슬로 명소가
  Day 7의 귀환 구간(1,079km 지점)에 잡혀 17개 지점이 전부 한 점으로 뭉쳤다. 그 장소가
  속한 **Day의 구간 안에서만** 찾아야 한다.
- **좌표가 정확히 겹치는 경우.** 플롬 숙소의 좌표가 플롬스바나 구간의 시작점과 완전히
  일치해서, 처음 플롬에 도착한 지점(734km)이 아니라 스테가스테인을 다녀온 뒤(746km)에
  잡혔다. 그 바람에 스테가스테인이 뒤로 밀렸다. 거리가 300m 이내로 비슷하면 **더 이른
  지점**을 고르도록 했다.

재생 시간은 프레임 루프를 그대로 시뮬레이션해 확인했다 — 1배속 2.7분, 4배속 1.4분,
두 경우 모두 17개 지점을 일정 순서대로 지나고 정상 종료한다.

#### 방향 화살표 구현 방식
`symbol-placement: 'line'`인 심볼 레이어를 경로 선 위에 얹는다. 선 자체는 건드리지 않는다.
- Day마다 색이 달라 `map.addImage()`로 Day 수만큼 화살표 이미지를 등록하고
  `icon-image`를 `['concat', 'route-arrow-', ['get','dayNumber']]`로 고른다.
- `icon-rotation-alignment: 'map'`이면 MapLibre가 선의 진행 방향(좌표 배열 순서)에 맞춰
  회전시킨다. 별도 각도 계산이 필요 없다.
- 흰 테두리가 필요한 이유: 파선(하이킹·페리·철도) 구간에서는 화살표가 선이 아니라 배경
  위에 놓인다. 테두리가 없으면 밝은 지도에서 옅은 색 화살표가 묻힌다.
- `setStyle`은 등록된 이미지도 지운다. `styledata`에서 레이어와 함께
  이미지도 다시 넣는다.
- 간격은 `symbol-spacing: 120`(px). 줄이면 촘촘해진다.

#### 훅으로 뺀 뒤에 생긴 정리(cleanup) 순서 버그

`useMapLibre`로 지도 생성을 분리한 뒤, 화면을 전환할 때마다 콘솔에 에러가 났다.

```
TypeError: Cannot read properties of undefined (reading 'off')
  at NavigationControl.onRemove → Map.removeControl → MapView.tsx:173
```

React는 effect의 **선언 순서대로** cleanup을 돌린다. `useMapLibre()`는 컴포넌트 맨 위에서
호출되므로 그 effect가 가장 먼저 등록되고, 따라서 언마운트 때 **가장 먼저 `map.remove()`가
실행된다.** 그 뒤에 도는 MapView의 cleanup이 이미 파괴된 지도에 `removeControl()`을
호출해 터졌다. 리팩터링 전에는 생성과 정리가 한 effect 안에 있어서 없던 문제다.

훅이 정리하면서 `mapRef.current`를 null로 만드는 것을 신호로 삼아, 뒤따르는 cleanup들은
`mapRef.current !== map`이면 그냥 빠져나오게 했다. 지도가 사라졌으면 거기 붙어 있던
컨트롤·팝업·마커도 함께 사라졌으므로 따로 치울 것이 없다.

교훈: 공유 훅이 리소스를 소유하면, 그 리소스에 의존하는 cleanup은 **리소스가 아직
살아 있는지 확인**해야 한다. 선언 순서상 훅의 cleanup이 항상 먼저 돈다.

#### 남은 문제 (기능에 지장 없음)
- 번들 1.3MB (gzip 374KB). 대부분 MapLibre다. 필요하면 코드 스플리팅.
- 공항 마커 2개(도착·출국)가 좌표가 같아 "전체" 보기에서 겹친다. FR-MAP-7(클러스터링)로 처리.
- Day 3의 경로 요약이 자동차+하이킹 합산이라 아이콘이 🥾로 뭉뚱그려진다. leg 목록에는 나뉘어 있다.
- 팝업과 패널이 같은 상세를 동시에 보여준다. 요구사항대로지만 중복이다.
- 짧은 구간(Day 5 베르겐 도보 1.2km)은 축소된 줌에서 화살표 간격에 걸리지 않아 표시되지
  않는다. 해당 Day를 열면 나타난다.
- 오슬로 시내 명소 4곳은 Day 1 경로가 공항→시내뿐이라 투어에서 거의 같은 지점에 정차한다.
  자동차는 제자리에 있고 카드만 넘어간다. 시내 도보 동선을 데이터에 넣으면 해결된다.
- 갔다가 되짚는 구간(트롤퉁가 등)은 같은 좌표 위에 진한 선과 연한 선이 겹친다. 하나의
  LineString이라 같은 픽셀에 두 상태를 동시에 보여줄 수 없다. 나중에 그린 쪽이 이긴다.
- 도시 구간에서는 그날의 도보 경로 옆에 다른 날의 도로가 나란히 지난다(베르겐은 Day 4
  도착·Day 6 출발 도로가 겹친다). 전부 Day 색이라 구분은 되지만 밀집해 보인다.

#### 진행률은 반드시 메르카토르 기준으로 (line-gradient)

`line-gradient`의 `line-progress`는 **투영 좌표계(메르카토르)에서 잰 길이 비율**이다.
지상 거리(km) 비율을 넘기면 색 경계가 자동차와 어긋난다. 메르카토르는 위도에 따라
1/cos(위도)만큼 늘어나므로, 위도 폭이 큰 경로일수록 차이가 커진다.

이 여정(위도 59.9°~61.1°)에서 실제로 측정한 어긋남:

| 지상 진행률 | 메르카토르 진행률 | 선 위 거리 차이 |
|---|---|---|
| 25% | 24.9% | 1.6km |
| 40% | 39.8% | 2.5km |
| 50.6% (베르겐 도보) | 50.4% | **2.5km** |
| 90% | 90.1% | -1.1km |

베르겐 시내 도보 구간은 전체가 1.2km다. 어긋남 2.5km는 그 두 배라, 자동차는 시내를
걷는데 색은 2.5km 떨어진 다른 도로에 칠해졌다. 겉보기엔 "상관없는 선이 칠해지는" 증상으로
나타난다.

`TourPoint.frac`에 메르카토르 누적 거리를 정규화해 담고, 그라디언트에는 이 값만 쓴다.
진행 막대는 "얼마나 왔나"를 뜻하므로 지상 거리 비율을 그대로 쓴다 — 둘은 다른 값이다.

#### 투어를 만들며 다시 밟은 지뢰

정차 마커에 `transition: transform`을 걸어, **상세 화면 마커에서 이미 고쳤던 버그를 그대로
재현했다.** MapLibre가 카메라 이동마다 그 요소의 `transform`으로 위치를 갱신하는데
transition이 걸려 있으면 마커가 150ms씩 뒤처져 헤엄치듯 흔들린다. 마커 바깥 요소의
`position`/`transform`은 MapLibre 전용으로 두고 시각 효과는 반드시 안쪽 래퍼에 건다.

이 실수를 계기로 지도 생성 로직을 `useMapLibre` 훅으로 묶어 두 화면이 공유하게 했다.
같은 함정을 화면마다 각자 밟지 않도록 하는 것이 목적이다.

또 하나: 조작 UI는 지도보다 먼저 뜬다. 지도가 준비되기 전에 정차 지점을 고르면 마커의
방문 표시와 자동차 아이콘이 초기값에 머문다. `ready`가 켜지는 시점에 현재 상태를 한 번
다시 그려서 맞춘다.

### Phase 1.5 — 완성도
- 3D terrain 토글, 마커 클러스터링
- ~~여정 애니메이션 재생~~ — 투어 화면으로 Phase 1에서 완료
- ~~경로 방향 화살표~~ · ~~URL 상태 동기화~~ — Phase 1에서 완료
- 메모 접기/더 보기(FR-NOTE-8), Day 탭 경고 표시(FR-NOTE-9), 메모 모아보기(FR-NOTE-10)

### Phase 2 — 사용자 입력
- 지도 클릭으로 장소 추가, Day 배정, 드래그로 순서 변경
- 장소 검색 (Nominatim 또는 Photon — 무료)
- 라우팅 API 연동으로 경로 자동 생성
- **메모 작성/수정/삭제 UI** — 상세 패널에서 인라인 편집, kind 선택
- 데이터 저장: localStorage → 백엔드(API + DB) 순으로 확장
- 여행 공유 링크, 내보내기(GPX/GeoJSON/JSON)

### Phase 3 — 확장 후보
- 여러 여행 관리, 협업 편집, 비용 집계, 예약 정보 연동, 오프라인 지원

## 8.5 영어화 (2026-08-28)

서비스 전체를 영어로 전환했다.

- **UI 문자열** — 컴포넌트의 라벨·aria-label·버튼, 날짜 형식(`Mon, Jul 6`),
  소요시간 형식(`1 hr 30 min`), WebGL 미지원 안내, `<html lang>`와 문서 제목.
- **여정 데이터** — 7일치 제목·요약, 24개 장소의 이름·설명·태그·비용·링크 라벨,
  모든 메모, leg 설명, `_meta`. 장소의 `nameLocal`(노르웨이어 표기)은 그대로 둔다.
  번역은 경로(day·place id)로 지정해 적용하고 항목 수가 어긋나면 실패하도록 해서,
  메모나 링크가 조용히 누락되는 일이 없게 했다.
- **코드 주석** — 전부 영어로. 지도 관련 함정을 설명하는 주석이 많아 내용은 보존했다.

문서(README.md, REQUIREMENTS.md)는 아직 한국어다.

### 모바일 하단 버튼 줄바꿈

`@media (max-width: 899px)` 안의 `.tour__btn { width: 30px }`가 파일 뒤쪽에 있어
`.tour__btn--wide { width: auto }`를 덮었다(같은 우선순위, 나중 선언이 이김). 그래서
`Overview` 라벨이 30px 안에 갇혀 두 줄로 깨졌다. 미디어쿼리에서 wide 변형을 제외하고
`white-space: nowrap`을 더했다. 같은 김에 시작 안내 카드가 좁게 shrink-wrap 되던 것도
`width: max-content`로 고쳤다.

## 9. 열린 질문 / 결정 필요

0. ~~**경로 지오메트리 생성 방법**~~ — 해결. `waypoints`를 사람이 관리하고
   `npm run routes`(scripts/build-routes.mjs)가 `geometry`를 생성한다. 재실행하면
   기존 형상이 그대로 재현되는 것까지 확인했다.
1. ~~**일정 내 시간 표현**~~ — 해결. `Place.time`(`"HH:MM"`)을 추가했다. 모든 지점에
   넣지 않고 시각이 실제로 중요한 곳에만 넣는다.
2. **Day 4(베르겐) 지점 미정** — 여행자가 정하면 채운다.
3. **숙소 3곳의 정확한 좌표** — Airbnb 예약은 실제지만 주소를 몰라 가장 가까운 지명에
   찍어 두었다.
4. **연도 확인** — 2026으로 넣었다. 오늘(2026-08-29) 기준으로 지난 주다.
5. **Trip 여러 개 지원 시점** — Phase 1부터 로더를 일반화할지, 하나로 하드코딩할지.

### 확정된 사항 (2026-08-28)

- 메모는 **작성자 본인의 메모**. 여러 명이 다는 댓글이 아님.
- 메모 단위는 **Place와 Day 두 곳**. 별도 Region 엔티티는 두지 않음.
- Phase 1은 **읽기 전용**, 메모는 JSON에 미리 작성. 편집 UI는 Phase 2.
- 지도에는 **마커 배지**로 존재만 알리고, 전문은 상세 팝업·일정 패널에 표시.
- 메모에 **종류 구분 있음** — `warning` / `tip` / `note`.
- **경로 지오메트리는 OSRM으로 한 번 뽑아 JSON에 고정** (7절 (b) 안). 시드 데이터에 이미
  적용 완료 — 실제 도로 형상 1,649점, 거리·소요시간도 라우팅 결과값.
- 첫 여정은 **오슬로 출발/도착 순환 코스**. 남서부(스타방에르·프레이케스톨렌) 안은 폐기.
- **베이스 타일은 OpenFreeMap `liberty`**. (다크 스타일도 확인했으나 다크모드 제거로 미사용.) 키 없이
  200 응답을 확인했다. 음영기복 래스터가 무거운 점은 `style.load` 사용으로 우회했다.

## 8.6 사용자 피드백 반영 (2026-08-30)

1. **다크모드 제거.** 가독성이 떨어진다는 이유. 팔레트(`:root[data-theme='dark']`),
   토글 버튼, 테마 상태, `useMapLibre`의 `setStyle` 전환 로직을 전부 걷어냈다.
   `useMapLibre`는 이제 스타일 URL 하나만 쓴다.
2. **따라가기 줌을 크게 넓혔다.** 자동차 7.6 → **6.9**. 가까이 붙어 쫓아가는 것보다
   경로 전체가 화면에 남아 있는 편이 여정을 이해하는 데 낫다는 피드백. 다른 이동수단도
   같은 비율로 낮췄다 (하이킹 11 → 9.5, 기차 10 → 8.5, 도보 13.5 → 12).
3. **모바일 카드가 긴 설명에서 잘렸다.** `max-height`를 38% → **62%** 로 올리고
   사진 높이를 116px → 104px로 줄였다. 검증 결과 세 카드 모두 내용이 박스 안에
   들어가고(401/399, 360/358, 261/259), 더 길어지면 스크롤된다.
